require 'spec_helper'

describe Invline do
  before(:each) do
    @valid_attributes = {
      
    }
  end

  it "should create a new instance given valid attributes" do
    Invline.create!(@valid_attributes)
  end
end
