%h1 Creating events

= error_messages_for :events

- form_for(:events, :url => objects_url) do |f|
  = render :partial => "form", :locals => {:f => f}
  %p= submit_tag "Create"

= link_to 'Back', objects_path
