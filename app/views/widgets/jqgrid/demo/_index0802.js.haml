:css
  #toolbar { padding: 0.3em; }
  #toolbar .ui-button{ font-size: 0.8em; }
  .demo-description td{ vertical-align: top; }

:javascript
  $(function() {
    $("a", "#toolbar").button();
  });
  
  $(document).ready(function() {
    $("#bedata").click( function(){
      var gr = $("#invheaders_demo0802").jqGrid('getGridParam','selrow');
      if( gr != null )
        $("#invheaders_demo0802").jqGrid('editGridRow',gr,{height:280,reloadAfterSubmit:false});
      else
       alert("Please Select Row");
    });
  });
  
= raw @grid