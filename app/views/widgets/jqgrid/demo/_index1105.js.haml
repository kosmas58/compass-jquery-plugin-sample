:css
  #toolbar { padding: 0.3em; }
  #toolbar .ui-button{ font-size: 0.8em; }

= raw @grid

:javascript
  $(function() {
    $("a", "#toolbar").button();
  });
  
  $(function() {
    $("#ttogrid").click( function() {
      tableToGrid("#mytable");;
    });
  });  
  