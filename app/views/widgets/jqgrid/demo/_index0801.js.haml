:css
  #toolbar { padding: 0.3em; }
  #toolbar .ui-button{ font-size: 0.8em; }
  .demo-description td{ vertical-align: top; }

:javascript
  $(function() {
    $("a", "#toolbar").button();
  });

  $(function() {
    $("#bsdata").click( function(){
      $("#invheaders_demo0801").jqGrid('searchGrid',
        {sopt:['cn','bw','eq','ne','lt','gt','ew']}
      );
    });
  });
  
= raw @grid