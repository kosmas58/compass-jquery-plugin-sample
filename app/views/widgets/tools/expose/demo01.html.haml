-@title = "#{t('txt.tools.name')} - #{t('txt.tools.toolbox.expose.name')} - #{t('txt.tools.toolbox.expose.demo01')}"

- content_for :head_scripts do
  = render :partial => "demo01.js.haml", :format => :js

.demo
  %h3
    =image_tag("icons/22x22/tools.png", :border => 0)
    =@title
  %p
    This document shows you the basics of setting up expose. You take one or more elements then call the
    %samp
      mask
    method and the magic happens. Here is a demo:
  #test
    Click on this element to expose it.
/ End demo

.demo-description 
  #greybox
/ End demo-description
