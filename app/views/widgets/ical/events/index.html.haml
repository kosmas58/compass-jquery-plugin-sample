-@title = t('txt.ical.event.listing')

- content_for :content_head do
  = render(:partial => "index.js.haml", :format => :js)

.demo
  %h3
    =image_tag("icons/22x22/calendar.png", :border => 0)
    =@title
    
  = render :partial => "events"
  
  %p.buttons  
    %button.event_create
      = t('txt.ical.event.new')
    = link_to t('txt.layout.back'), widgets_ical_calendars_path
    
  #event_delete_dialog{ :style => "display: none;" }
  #event_update_dialog{ :style => "display: none;" }
  #event_create_dialog{ :style => "display: none;" }
/ End demo
 