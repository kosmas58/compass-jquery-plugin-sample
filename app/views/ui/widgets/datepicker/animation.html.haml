-@title = "#{t('txt.ui.name')} #{t('txt.ui.widgets.datepicker.default')} - #{t('txt.ui.widgets.datepicker.animation')}"

- content_for :content_head do
  = render :partial => "animation.js.haml", :format => :js
    
.demo
  %h3
    =image_tag("icons/22x22/jquery-ui.png", :border => 0)
    =@title
  %p
    Animations:
    %br
    %br
    %select#anim
      %option{ :value => "show" }
        Show (default)
      %option{ :value => "slideDown" }
        Slide down
      %option{ :value => "fadeIn" }
        Fade in
      /
        <option value="blind">Blind (UI Effect)</option>
        <option value="bounce">Bounce (UI Effect)</option>
        <option value="clip">Clip (UI Effect)</option>
        <option value="drop">Drop (UI Effect)</option>
        <option value="fade">Fade (UI Effect)</option>
        <option value="fold">Fold (UI Effect)</option>
        <option value="slide">Slide (UI Effect)</option>
      %option{ :value => "" }
        None
    /
  %p
    Date:
    %input#datepicker{ :size => "30", :type => "text" }
/ End demo
.demo-description
  %p
    Use different animations when opening or closing the datepicker.
    Choose an animation from the dropdown, then click on the input to see its effect.
    You can use one of the three standard animations or any of the UI Effects.
/ End demo-description
