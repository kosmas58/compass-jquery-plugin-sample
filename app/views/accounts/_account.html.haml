%div[account]

  = link_to 'Show', object_path(account)
  |
  = link_to 'Edit', edit_object_path(account)
  |
  = link_to 'Destroy', object_path(account), :confirm => 'Really destroy account?', :method => :delete
