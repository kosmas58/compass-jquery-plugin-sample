:javascript
  $(function() {
    $("#tabs-1").tabs({
      ajaxOptions: {
        error: function(xhr, status, index, anchor) {
          $(anchor.hash).html("Couldn't load this tab. We'll try to fix this as soon as possible. If this wouldn't be a demo.");
        }
      }
    });
  });

.demo
  #tabs-1
    %ul.ajax-links
      %li
        %a.default-content{ :href => "load_1a" }
          load 1a.html
      %li
        %a{ :href => "load_1b" }
          load 1b.html
      %li
        %a{ :href => "load_1c" }
          load 1c.html
