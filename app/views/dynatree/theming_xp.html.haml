-@title = "#{t('txt.dt.name')} - #{t('txt.dt.theming.name')} - #{t('txt.dt.theming.xp')}"
-@dynatree_theme = 'redmond'
-@dynatree_skin = 'xp.css'

- content_for :content_head do
  = render(:partial => "theming.js.haml", :format => :js)

.demo
  .div.ui-widget-content.ui-corner-all
    %h4.ui-widget-header.ui-corner-all
      =@title
    #tree{ :style => "padding: 20px;" }
/ End demo

.demo-description
  %p
    Includes a custom CSS for a new theming (inspired by Windows XP).
    %br
    Some nodes have their
    %code
      data.addClass
    attribute set.
    %br
    Finally, the last two nodes use the
    %code
      data.icon
    attribute.
/ End demo-description
