- scale = (session[:ppi]/218.49).round_to(2)
- content_for :content_head do
  = render :partial => "/emulators/scale.js.haml", :format => :js, :locals => { :scale => scale }

= render :partial => "emulate", :locals => {:url => "http://www.jqtouch.com/preview/demos/main/", :scale => scale }
