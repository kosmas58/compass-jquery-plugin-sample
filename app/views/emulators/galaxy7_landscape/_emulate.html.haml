%center
  .galaxy7
    .emulator
      .display-area
        %iframe{ :src => url, :width => "1024", :height => "564", :frameborder => "0" }
        %a{ :href => url }
          Click here
        if the IFrame could not be shown.
