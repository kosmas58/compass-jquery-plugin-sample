.span-24.last
  #toolbar.ui-widget-header.ui-corner-all
    %button#rotate
    %button#scroll
    %button#resize
    %button#configure_ppi
  %center
    .emulator
      .display-area
        %iframe#disp-area{ :src => url, :width => width, :height => height, :scrolling => cookies[:scrolling] }
