%center
  .galaxy10
    .emulator
      .display-area
        %iframe{ :src => url, :width => "800", :height => "1188", :frameborder => "0" }
        %a{ :href => url }
          Click here
        if the IFrame could not be shown.