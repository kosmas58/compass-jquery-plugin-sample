!!!
%html{:lang => "en"}
  %head
    %meta{:content => "text/html; charset=UTF-8", "http-equiv" => "content-type"}/
  %body
    %div{"data-role" => "page"}
      %div{"data-role" => "header"}
        %h1 .clearQueue()
      #clearQueue1{"data-role" => "content", "data-theme" => "c"}
        %h2.jq-clearfix.roundTop.section-title
          %span.name .clearQueue( [ queueName ] )
          %span.returns
            Returns:
            %a.return{:href => "http://docs.jquery.com/Types#jQuery"} jQuery
        .entry-details
          %p.desc
            %strong> Description:
            Remove from the queue all items that have not yet been run.
          %ul.signatures
            %li#clearQueue-queueName.signature
              %h4.name
                %span.versionAdded>
                  version added:
                  %a{:href => "/category/version/1.4/"} 1.4
                \.clearQueue( [ queueName ] )
              %p.arguement
                %strong> queueName
                A string containing the name of the queue. Defaults to
                = succeed "," do
                  %code fx
                the standard effects queue.
          .longdesc
            %p
              When the
              %code .clearQueue()
              method is called, all functions on the queue that have not been executed are removed from the queue. When used without an argument,
              %code .clearQueue()
              removes the remaining functions from
              = succeed "," do
                %code fx
              the standard effects queue. In this way it is similar to
              = succeed "." do
                %code .stop(true)
              However,  while the
              %code .stop()
              method is meant to be used only with animations,
              %code .clearQueue()
              can also be used to remove any function that has been added to a generic jQuery queue with the
              %code .queue()
              method.
          %h3 Example:
          #entry-examples.entry-examples
            #example-0
              %h4
                %span.desc Empty the queue.
              %pre
                %code.example.demo-code
                  :preserve
                    &lt;!DOCTYPE html&gt;
                    &lt;html&gt;
                    &lt;head&gt;
                      &lt;style&gt;
                    div { margin:3px; width:40px; height:40px;
                        position:absolute; left:0px; top:30px; 
                        background:green; display:none; }
                    div.newcolor { background:blue; }
                    &lt;/style&gt;
                      &lt;script src="http://code.jquery.com/jquery-latest.js"&gt;&lt;/script&gt;
                    &lt;/head&gt;
                    &lt;body&gt;
                    	&lt;button id="start"&gt;Start&lt;/button&gt;
                    &lt;button id="stop"&gt;Stop&lt;/button&gt;
                    &lt;div&gt;&lt;/div&gt;
                    &lt;script&gt;$("#start").click(function () {
                      $("div").show("slow");
                      $("div").animate({left:'+=200'},5000);
                      $("div").queue(function () {
                        $(this).addClass("newcolor");
                        $(this).dequeue();
                      });
                      $("div").animate({left:'-=200'},1500);
                      $("div").queue(function () {
                        $(this).removeClass("newcolor");
                        $(this).dequeue();
                      });
                      $("div").slideUp();
                    });
                    $("#stop").click(function () {
                      $("div").clearQueue();
                      $("div").stop();
                    });&lt;/script&gt;
                    &lt;/body&gt;
                    &lt;/html&gt;              %h4 Demo:
              .demo.code-demo
