!!!
%html{:lang => "en"}
  %head
    %meta{:content => "text/html; charset=UTF-8", "http-equiv" => "content-type"}/
  %body
    %div{"data-role" => "page"}
      %div{"data-role" => "header"}
        %h1 jQuery.removeData()
      %div{"data-role" => "content", "data-theme" => "c", :id => "jQuery.removeData1"}
        %h2.jq-clearfix.roundTop.section-title
          %span.name jQuery.removeData( element, [ name ] )
          %span.returns
            Returns:
            %a.return{:href => "http://docs.jquery.com/Types#jQuery"} jQuery
        .entry-details
          %p.desc
            %strong> Description:
            Remove a previously-stored piece of data.
          %ul.signatures
            %li.signature{:id => "jQuery.removeData-element-name"}
              %h4.name
                %span.versionAdded>
                  version added:
                  %a{:href => "/category/version/1.2.3/"} 1.2.3
                jQuery.removeData( element, [ name ] )
              %p.arguement
                %strong> element
                A DOM element from which to remove data.
              %p.arguement
                %strong> name
                A string naming the piece of data to remove.
          .longdesc
            %p
              %strong Note:
              This is a low-level method, you should probably use
              %code
                %a{:href => "/removeData"} .removeData()
              instead.
            %p
              The
              %code jQuery.removeData()
              method allows us to remove values that were previously set using
              = succeed "." do
                %code
                  %a{:href => "/jQuery.data"} jQuery.data()
              When called with the name of a key,
              %code jQuery.removeData()
              deletes that particular value; when called with no arguments, all values are removed.
          %h3 Example:
          #entry-examples.entry-examples
            #example-0
              %h4
                %span.desc Set a data store for 2 names then remove one of them.
              %pre
                %code.example.demo-code
                  :preserve
                    &lt;!DOCTYPE html&gt;
                    &lt;html&gt;
                    &lt;head&gt;
                      &lt;style&gt;
                    		div { margin:2px; color:blue; }
                    		span { color:red; }
                    		&lt;/style&gt;
                      &lt;script src="http://code.jquery.com/jquery-latest.js"&gt;&lt;/script&gt;
                    &lt;/head&gt;
                    &lt;body&gt;
                    	&lt;div&gt;value1 before creation: &lt;span&gt;&lt;/span&gt;&lt;/div&gt;
                    		&lt;div&gt;value1 after creation: &lt;span&gt;&lt;/span&gt;&lt;/div&gt;
                    		&lt;div&gt;value1 after removal: &lt;span&gt;&lt;/span&gt;&lt;/div&gt;
                    		&lt;div&gt;value2 after removal: &lt;span&gt;&lt;/span&gt;&lt;/div&gt;
                    &lt;script&gt;
                    var div = $("div")[0];
                    $("span:eq(0)").text("" + $("div").data("test1"));
                    jQuery.data(div, "test1", "VALUE-1");
                    jQuery.data(div, "test2", "VALUE-2");
                    $("span:eq(1)").text("" + jQuery.data(div, "test1"));
                    jQuery.removeData(div, "test1");
                    $("span:eq(2)").text("" + jQuery.data(div, "test1"));
                    $("span:eq(3)").text("" + jQuery.data(div, "test2"));&lt;/script&gt;
                    &lt;/body&gt;
                    &lt;/html&gt;              %h4 Demo:
              .demo.code-demo
