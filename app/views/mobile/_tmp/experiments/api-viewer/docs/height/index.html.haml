!!!
%html{:lang => "en"}
  %head
    %meta{:charset => "utf-8"}/
    %meta{:content => "text/html; charset=UTF-8", "http-equiv" => "content-type"}/
  %body
    %div{"data-role" => "page"}
      %div{"data-role" => "header"}
        %h1 .height()
      #height1{"data-role" => "content", "data-theme" => "c"}
        %h2.jq-clearfix.roundTop.section-title
          %span.name .height()
          %span.returns
            Returns:
            %a.return{:href => "http://docs.jquery.com/Types#Integer"} Integer
        .entry-details
          %p.desc
            %strong> Description:
            Get the current computed height for the first element in the set of matched elements.
          %ul.signatures
            %li#height.signature
              %h4.name
                %span.versionAdded>
                  version added:
                  %a{:href => "/category/version/1.0/"} 1.0
                \.height()
          .longdesc
            %p
              The difference between
              %code .css('height')
              and
              %code .height()
              is that the latter returns a unit-less pixel value (for example,
              = succeed ")" do
                %code 400
              while the former returns a value with units intact (for example,
              = succeed ")." do
                %code 400px
              The
              %code .height()
              method is recommended when an element's height needs to be used in a mathematical calculation.
            %p.image
              %img{:src => "http://api.jquery.com/images/0042_04_01.png"}/
            %p This method is also able to find the height of the window and document.
            %pre
              :preserve
                $(window).height();   // returns height of browser viewport
                $(document).height(); // returns height of HTML document          %h3 Example:
          #entry-examples.entry-examples
            #example-0
              %h4
                %span.desc Show various heights.  Note the values are from the iframe so might be smaller than you expected.  The yellow highlight shows the iframe body.
              %pre
                %code.example.demo-code
                  :preserve
                    &lt;!DOCTYPE html&gt;
                    &lt;html&gt;
                    &lt;head&gt;
                      &lt;style&gt;
                      body { background:yellow; }
                      button { font-size:12px; margin:2px; }
                      p { width:150px; border:1px red solid; }
                      div { color:red; font-weight:bold; }
                      &lt;/style&gt;
                      &lt;script src="http://code.jquery.com/jquery-latest.js"&gt;&lt;/script&gt;
                    &lt;/head&gt;
                    &lt;body&gt;
                    	&lt;button id="getp"&gt;Get Paragraph Height&lt;/button&gt;
                      &lt;button id="getd"&gt;Get Document Height&lt;/button&gt;
                      &lt;button id="getw"&gt;Get Window Height&lt;/button&gt;
                    
                      &lt;div&gt;&amp;nbsp;&lt;/div&gt;
                      &lt;p&gt;
                        Sample paragraph to test height
                      &lt;/p&gt;
                    &lt;script&gt;
                        function showHeight(ele, h) {
                          $("div").text("The height for the " + ele + 
                                        " is " + h + "px.");
                        }
                        $("#getp").click(function () { 
                          showHeight("paragraph", $("p").height()); 
                        });
                        $("#getd").click(function () { 
                          showHeight("document", $(document).height()); 
                        });
                        $("#getw").click(function () { 
                          showHeight("window", $(window).height()); 
                        });
                    
                    &lt;/script&gt;
                    &lt;/body&gt;
                    &lt;/html&gt;              %h4 Demo:
              .demo.code-demo
      #height2{"data-role" => "content", "data-theme" => "c"}
        %h2.jq-clearfix.roundTop.section-title
          %span.name .height( value )
          %span.returns
            Returns:
            %a.return{:href => "http://docs.jquery.com/Types#jQuery"} jQuery
        .entry-details
          %p.desc
            %strong> Description:
            Set the CSS height of every matched element.
          %ul.signatures
            %li#height-value.signature
              %h4.name
                %span.versionAdded>
                  version added:
                  %a{:href => "/category/version/1.0/"} 1.0
                \.height( value )
              %p.arguement
                %strong> value
                An integer representing the number of pixels, or an integer with an optional unit of measure appended (as a string).
            %li.signature{:id => "height-functionindex, height"}
              %h4.name
                %span.versionAdded>
                  version added:
                  %a{:href => "/category/version/1.4.1/"} 1.4.1
                \.height( function(index, height) )
              %p.arguement
                %strong> function(index, height)
                A function returning the height to set. Receives the index position of the element in the set and the old height as arguments.
          .longdesc
            %p
              When calling
              = succeed "," do
                %code .height(value)
              the value can be either a string (number and unit) or a number. If only a number is provided for the value, jQuery assumes a pixel unit. If a string is provided, however, any valid CSS measurement may be used for the height (such as
              = succeed "," do
                %code 100px
              = succeed "," do
                %code 50%
              or
              = succeed ")." do
                %code auto
              Note that in modern browsers, the CSS height property does not include padding, border, or margin.
            %p If no explicit unit was specified (like 'em' or '%') then "px" is concatenated to the value.
          %h3 Example:
          #entry-examples.entry-examples
            #example-0
              %h4
                %span.desc To set the height of each div on click to 30px plus a color change.
              %pre
                %code.example.demo-code
                  :preserve
                    &lt;!DOCTYPE html&gt;
                    &lt;html&gt;
                    &lt;head&gt;
                      &lt;style&gt;div { width:50px; height:70px; float:left; margin:5px;
                            background:rgb(255,140,0); cursor:pointer; }  &lt;/style&gt;
                      &lt;script src="http://code.jquery.com/jquery-latest.js"&gt;&lt;/script&gt;
                    &lt;/head&gt;
                    &lt;body&gt;
                    	&lt;div&gt;&lt;/div&gt;
                      &lt;div&gt;&lt;/div&gt;
                    
                      &lt;div&gt;&lt;/div&gt;
                      &lt;div&gt;&lt;/div&gt;
                      &lt;div&gt;&lt;/div&gt;
                    &lt;script&gt;$("div").one('click', function () {
                          $(this).height(30)
                                 .css({cursor:"auto", backgroundColor:"green"});
                        });&lt;/script&gt;
                    &lt;/body&gt;
                    &lt;/html&gt;              %h4 Demo:
              .demo.code-demo
