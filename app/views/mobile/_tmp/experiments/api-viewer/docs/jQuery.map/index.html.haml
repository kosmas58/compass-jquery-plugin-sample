!!!
%html{:lang => "en"}
  %head
    %meta{:charset => "utf-8"}/
    %meta{:content => "text/html; charset=UTF-8", "http-equiv" => "content-type"}/
  %body
    %div{"data-role" => "page"}
      %div{"data-role" => "header"}
        %h1 jQuery.map()
      %div{"data-role" => "content", "data-theme" => "c", :id => "jQuery.map1"}
        %h2.jq-clearfix.roundTop.section-title
          %span.name jQuery.map( array, callback(elementOfArray, indexInArray) )
          %span.returns
            Returns:
            %a.return{:href => "http://docs.jquery.com/Types#Array"} Array
        .entry-details
          %p.desc
            %strong> Description:
            Translate all items in an array or array-like object to another array of items.
          %ul.signatures
            %li.signature{:id => "jQuery.map-array-callbackelementOfArray, indexInArray"}
              %h4.name
                %span.versionAdded>
                  version added:
                  %a{:href => "/category/version/1.0/"} 1.0
                jQuery.map( array, callback(elementOfArray, indexInArray) )
              %p.arguement
                %strong> array
                The Array to translate.
              %p.arguement
                %strong> callback(elementOfArray, indexInArray)
                The function to process each item against.  The first argument to the function is the list item, the second argument is the index in array The function can return any value.
                %code this
                will be the global window object.
          .longdesc
            %p The $.map() method applies a function to each item in an array, collecting the results into a new array.
            %p The translation function that is provided to this method is called for each item in the array and is passed two arguments: The item to be translated, and the index within the array.
            %p The function can return:
            %ul
              %li the translated value, which will be mapped to the resulting array
              %li
                %code> null
                , to remove the item
              %li an array of values, which will be flattened into the full array
            %p Map can  iterate through Array-like objects, like a jQuery object, that have a length property.
          %h3 Examples:
          #entry-examples.entry-examples
            #example-0
              %h4
                Example:
                %span.desc A couple examples of using .map()
              %pre
                %code.example.demo-code
                  :preserve
                    &lt;!DOCTYPE html&gt;
                    &lt;html&gt;
                    &lt;head&gt;
                      &lt;style&gt;
                      div { color:blue; }
                      p { color:green; margin:0; }
                      span { color:red; }
                      &lt;/style&gt;
                      &lt;script src="http://code.jquery.com/jquery-latest.js"&gt;&lt;/script&gt;
                    &lt;/head&gt;
                    &lt;body&gt;
                    	&lt;div&gt;&lt;/div&gt;
                      &lt;p&gt;&lt;/p&gt;
                      &lt;span&gt;&lt;/span&gt;
                      
                    &lt;script&gt;
                    
                        var arr = [ "a", "b", "c", "d", "e" ];
                        $("div").text(arr.join(", "));
                    
                        arr = jQuery.map(arr, function(n, i){
                          return (n.toUpperCase() + i);
                        });
                        $("p").text(arr.join(", "));
                    
                        arr = jQuery.map(arr, function (a) { return a + a; });
                        $("span").text(arr.join(", "));
                    
                    &lt;/script&gt;
                    &lt;/body&gt;
                    &lt;/html&gt;              %h4 Demo:
              .demo.code-demo
            #example-1
              %h4
                Example:
                %span.desc Maps the original array to a new one and adds 4 to each value.
              %pre
                %code.example
                  :preserve
                    $.map( [0,1,2], function(n){
                       return n + 4;
                     });              %h4 Result:
              %pre
                %code.results [4, 5, 6]
            #example-2
              %h4
                Example:
                %span.desc Maps the original array to a new one and adds 1 to each value if it is bigger then zero, otherwise it's removed.
              %pre
                %code.example
                  :preserve
                    $.map( [0,1,2], function(n){
                       return n &gt; 0 ? n + 1 : null;
                     });              %h4 Result:
              %pre
                %code.results [2, 3]
            #example-3
              %h4
                Example:
                %span.desc Maps the original array to a new one, each element is added with it's original value and the value plus one.
              %pre
                %code.example
                  :preserve
                    $.map( [0,1,2], function(n){
                       return [ n, n + 1 ];
                     });              %h4 Result:
              %pre
                %code.results [0, 1, 1, 2, 2, 3]
            #example-4
              %h4
                Example:
                %span.desc Maps the original array to a new one, each element is squared.
              %pre
                %code.example $.map( [0,1,2,3], function (a) { return a * a; } );
              %h4 Result:
              %pre
                %code.results [0, 1, 4, 9]
            #example-5
              %h4
                Example:
                %span.desc Remove items by returning null from the function. This removes any numbers less than 50, and the rest are decreased by 45.
              %pre
                %code.example $.map( [0, 1, 52, 97], function (a) { return (a &gt; 50 ? a - 45 : null); } );
              %h4 Result:
              %pre
                %code.results [7, 52]
            #example-6
              %h4
                Example:
                %span.desc Augmenting the resulting array by returning an array inside the function.
              %pre
                %code.example
                  :preserve
                    var array = [0, 1, 52, 97];
                    array = $.map(array, function(a, index) {
                      return [a - 45, index];
                    });               %h4 Result:
              %pre
                %code.results [-45, 0, -44, 1, 7, 2, 52, 3]
