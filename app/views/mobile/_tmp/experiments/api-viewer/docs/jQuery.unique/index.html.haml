!!!
%html{:lang => "en"}
  %head
    %meta{:charset => "utf-8"}/
    %meta{:content => "text/html; charset=UTF-8", "http-equiv" => "content-type"}/
  %body
    %div{"data-role" => "page"}
      %div{"data-role" => "header"}
        %h1 jQuery.unique()
      %div{"data-role" => "content", "data-theme" => "c", :id => "jQuery.unique1"}
        %h2.jq-clearfix.roundTop.section-title
          %span.name jQuery.unique( array )
          %span.returns
            Returns:
            %a.return{:href => "http://docs.jquery.com/Types#Array"} Array
        .entry-details
          %p.desc
            %strong> Description:
            Sorts an array of DOM elements, in place, with the duplicates removed. Note that this only works on arrays of DOM elements, not strings or numbers.
          %ul.signatures
            %li.signature{:id => "jQuery.unique-array"}
              %h4.name
                %span.versionAdded>
                  version added:
                  %a{:href => "/category/version/1.1.3/"} 1.1.3
                jQuery.unique( array )
              %p.arguement
                %strong> array
                The Array of DOM elements.
          .longdesc
            %p
              The
              %code $.unique()
              function searches through an array of objects, sorting the array, and removing any duplicate nodes. This function only works on plain JavaScript arrays of DOM elements, and is chiefly used internally by jQuery.
            %p As of jQuery 1.4 the results will always be returned in document order.
          %h3 Example:
          #entry-examples.entry-examples
            #example-0
              %h4
                %span.desc Removes any duplicate elements from the array of divs.
              %pre
                %code.example.demo-code
                  :preserve
                    &lt;!DOCTYPE html&gt;
                    &lt;html&gt;
                    &lt;head&gt;
                      &lt;style&gt;
                      div { color:blue; }
                      &lt;/style&gt;
                      &lt;script src="http://code.jquery.com/jquery-latest.js"&gt;&lt;/script&gt;
                    &lt;/head&gt;
                    &lt;body&gt;
                    	&lt;div&gt;There are 6 divs in this document.&lt;/div&gt;
                      &lt;div&gt;&lt;/div&gt;
                      &lt;div class="dup"&gt;&lt;/div&gt;
                      &lt;div class="dup"&gt;&lt;/div&gt;
                    
                      &lt;div class="dup"&gt;&lt;/div&gt;
                      &lt;div&gt;&lt;/div&gt;
                    &lt;script&gt;
                    
                        var divs = $("div").get(); // unique() must take a native array
                    
                        // add 3 elements of class dup too (they are divs)
                        divs = divs.concat($(".dup").get());
                        $("div:eq(1)").text("Pre-unique there are " + divs.length + " elements.");
                    
                        divs = jQuery.unique(divs);
                        $("div:eq(2)").text("Post-unique there are " + divs.length + " elements.")
                                      .css("color", "red");
                    
                    &lt;/script&gt;
                    &lt;/body&gt;
                    &lt;/html&gt;              %h4 Demo:
              .demo.code-demo
