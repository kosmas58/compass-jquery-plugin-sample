%div{"data-role" => "page"}
  %div{"data-role" => "header"}
    %h1 event.which
  %div{"data-role" => "content", "data-theme" => "c", :id => "event.which1"}
    %h2.jq-clearfix.roundTop.section-title
      %span.name event.which
      %span.returns
        Returns:
        %a.return{:href => "http://docs.jquery.com/Types#String"} String
    .entry-details
      %p.desc
        %strong Description:
        For key or button events, this attribute indicates the specific button or key that was pressed.
      %ul.signatures
        %li.signature{:id => "event.which"}
          %h4.name
            %span.versionAdded>
              version added:
              %a{:href => "/category/version/1.1.3/"} 1.1.3
            event.which
      .longdesc
        %p
          %code event.which
          normalizes
          %code event.keyCode
          and
          = succeed "." do
            %code event.charCode
          It is recommended to watch
          %code event.which
          for keyboard key input. For more detail, read about
          = succeed "." do
            %a{:href => "http://developer.mozilla.org/en/DOM/event.charCode#Notes"} event.charCode on the MDC
      %h3 Example:
      #entry-examples.entry-examples
        #example-0
          %h4
            %span.desc Log what key was depressed.
          %pre
            %code.example.demo-code
              :preserve
                &lt;!DOCTYPE html&gt;
                &lt;html&gt;
                &lt;head&gt;
                  &lt;script src="http://code.jquery.com/jquery-latest.js"&gt;&lt;/script&gt;
                &lt;/head&gt;
                &lt;body&gt;
                	
                &lt;input id="whichkey" value="type something"&gt;
                &lt;div id="log"&gt;&lt;/div&gt;
                &lt;script&gt;$('#whichkey').bind('keydown',function(e){ 
                  $('#log').html(e.type + ': ' +  e.which );
                });  &lt;/script&gt;
                &lt;/body&gt;
                &lt;/html&gt;          %h4 Demo:
          .demo.code-demo
          %h4 Result:
          %pre
            %code.results "keydown" 74
