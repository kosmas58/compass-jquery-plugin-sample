%div{"data-role" => "page"}
  %div{"data-role" => "header"}
    %h1 jQuery.isEmptyObject()
  %div{"data-role" => "content", "data-theme" => "c", :id => "jQuery.isEmptyObject1"}
    %h2.jq-clearfix.roundTop.section-title
      %span.name jQuery.isEmptyObject( object )
      %span.returns
        Returns:
        %a.return{:href => "http://docs.jquery.com/Types#Boolean"} Boolean
    .entry-details
      %p.desc
        %strong> Description:
        Check to see if an object is empty (contains no properties).
      %ul.signatures
        %li.signature{:id => "jQuery.isEmptyObject-object"}
          %h4.name
            %span.versionAdded>
              version added:
              %a{:href => "/category/version/1.4/"} 1.4
            jQuery.isEmptyObject( object )
          %p.arguement
            %strong> object
            The object that will be checked to see if it's empty.
      .longdesc
        %p As of jQuery 1.4 this method checks both properties on the object itself and properties inherited from prototypes (in that it doesn't use hasOwnProperty).
      %h3 Example:
      #entry-examples.entry-examples
        #example-0
          %h4
            %span.desc Check an object to see if it's empty.
          %pre
            %code.example
              :preserve
                jQuery.isEmptyObject({}) // true
                jQuery.isEmptyObject({ foo: "bar" }) // false