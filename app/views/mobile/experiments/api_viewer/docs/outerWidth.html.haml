%div{"data-role" => "page"}
  %div{"data-role" => "header"}
    %h1 .outerWidth()
  #outerWidth1{"data-role" => "content", "data-theme" => "c"}
    %h2.jq-clearfix.roundTop.section-title
      %span.name .outerWidth( [ includeMargin ] )
      %span.returns
        Returns:
        %a.return{:href => "http://docs.jquery.com/Types#Integer"} Integer
    .entry-details
      %p.desc
        %strong> Description:
        Get the current computed width for the first element in the set of matched elements, including padding and border.
      %ul.signatures
        %li#outerWidth-includeMargin.signature
          %h4.name
            %span.versionAdded>
              version added:
              %a{:href => "/category/version/1.2.6/"} 1.2.6
            \.outerWidth( [ includeMargin ] )
          %p.arguement
            %strong> includeMargin
            A Boolean indicating whether to include the element's margin in the calculation.
      .longdesc
        %p Returns the width of the element, along with left and right padding, border, and optionally margin, in pixels.
        %p
          If
          %code includeMargin
          is omitted or
          = succeed "," do
            %code false
          the padding and border are included in the calculation; if
          = succeed "," do
            %code true
          the margin is also included.
        %p
          This method is not applicable to
          %code window
          and
          %code document
          objects; for these, use
          %code
            %a{:href => "/width"} .width()
          instead.
        %p.image
          %img{:src => "http://api.jquery.com/images/0042_04_06.png"}/
      %h3 Example:
      #entry-examples.entry-examples
        #example-0
          %h4
            %span.desc Get the outerWidth of a paragraph.
          %pre
            %code.example.demo-code
              :preserve
                &lt;!DOCTYPE html&gt;
                &lt;html&gt;
                &lt;head&gt;
                  &lt;style&gt;
                  p { margin:10px;padding:5px;border:2px solid #666; }
                  &lt;/style&gt;
                  &lt;script src="http://code.jquery.com/jquery-latest.js"&gt;&lt;/script&gt;
                &lt;/head&gt;
                &lt;body&gt;
                	&lt;p&gt;Hello&lt;/p&gt;&lt;p&gt;&lt;/p&gt;
                &lt;script&gt;var p = $("p:first");
                $("p:last").text( "outerWidth:" + p.outerWidth()+ " , outerWidth(true):" + p.outerWidth(true) );
                
                &lt;/script&gt;
                &lt;/body&gt;
                &lt;/html&gt;          %h4 Demo:
          .demo.code-demo
