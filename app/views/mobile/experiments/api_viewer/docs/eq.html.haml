%div{"data-role" => "page"}
  %div{"data-role" => "header"}
    %h1 .eq()
  #eq1{"data-role" => "content", "data-theme" => "c"}
    %h2.jq-clearfix.roundTop.section-title
      %span.name .eq( index )
      %span.returns
        Returns:
        %a.return{:href => "http://docs.jquery.com/Types#jQuery"} jQuery
    .entry-details
      %p.desc
        %strong> Description:
        Reduce the set of matched elements to the one at the specified index.
      %ul.signatures
        %li#eq-index.signature
          %h4.name
            %span.versionAdded>
              version added:
              %a{:href => "/category/version/1.1.2/"} 1.1.2
            \.eq( index )
          %p.arguement
            %strong> index
            An integer indicating the 0-based position of the element.
        %li#eq--index.signature
          %h4.name
            %span.versionAdded>
              version added:
              %a{:href => "/category/version/1.4/"} 1.4
            \.eq( -index )
          %p.arguement
            %strong> -index
            An integer indicating the position of the element, counting backwards  from the last element in the set.
      .longdesc
        %p
          Given a jQuery object that represents a set of DOM elements, the
          %code .eq()
          method constructs a new jQuery object from one of the matching elements. The supplied index identifies the position of this element in the set.
        %p Consider a page with a simple list on it:
        %pre
          :preserve
            
              &lt;ul&gt;
                &lt;li&gt;list item 1&lt;/li&gt;
                &lt;li&gt;list item 2&lt;/li&gt;
                &lt;li&gt;list item 3&lt;/li&gt;
                &lt;li&gt;list item 4&lt;/li&gt;
                &lt;li&gt;list item 5&lt;/li&gt;
              &lt;/ul&gt;
        %p We can apply this method to the set of list items:
        %pre
          :preserve
            
              $('li').eq(2).css('background-color', 'red');
        %p The result of this call is a red background for item 3. Note that the supplied index is zero-based, and refers to the position of the element within the jQuery object, not within the DOM tree.
        %p If a negative number is provided, this indicates a position starting from the end of the set, rather than the beginning. For example:
        %pre
          :preserve
            
              $('li').eq(-2).css('background-color', 'red');
        %p This time list item 4 is turned red, since it is two from the end of the set.
      %h3 Example:
      #entry-examples.entry-examples
        #example-0
          %h4
            %span.desc Turn the div with index 2 blue by adding an appropriate class.
          %pre
            %code.example.demo-code
              :preserve
                &lt;!DOCTYPE html&gt;
                &lt;html&gt;
                &lt;head&gt;
                  &lt;style&gt;
                  div { width:60px; height:60px; margin:10px; float:left;
                        border:2px solid blue; }
                  .blue { background:blue; }
                  &lt;/style&gt;
                  &lt;script src="http://code.jquery.com/jquery-latest.js"&gt;&lt;/script&gt;
                &lt;/head&gt;
                &lt;body&gt;
                	&lt;div&gt;&lt;/div&gt;
                  &lt;div&gt;&lt;/div&gt;
                  &lt;div&gt;&lt;/div&gt;
                
                  &lt;div&gt;&lt;/div&gt;
                  &lt;div&gt;&lt;/div&gt;
                  &lt;div&gt;&lt;/div&gt;
                &lt;script&gt;
                
                    $("div").eq(2).addClass("blue");
                &lt;/script&gt;
                &lt;/body&gt;
                &lt;/html&gt;          %h4 Demo:
          .demo.code-demo
