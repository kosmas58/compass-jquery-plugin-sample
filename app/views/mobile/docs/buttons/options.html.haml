-@title = "jQuery Mobile Docs - Button types"
= render :partial => "/mobile/index.js.haml", :format => :js

.type-interior{"data-role" => "page"}
  %div{"data-role" => "header", "data-theme" => "f"}
    %h1 Button basics
    %a.ui-btn-right.jqm-home{"data-direction" => "reverse", "data-icon" => "home", "data-iconpos" => "notext", :href => "/mobile"} Home
  / /header
  %div{"data-role" => "content"}
    .content-primary
      %h2 Button basics
      %ul.localnav{"data-role" => "controlgroup", "data-type" => "horizontal"}
        %li
          %a{"data-role" => "button", "data-transition" => "fade", :href => "/mobile/docs/buttons/types"} Basics
        %li
          %a.ui-btn-active{"data-role" => "button", "data-transition" => "fade", :href => "/mobile/docs/buttons/options"} Options
        %li
          %a{"data-role" => "button", "data-transition" => "fade", :href => "/mobile/docs/buttons/methods"} Methods
        %li
          %a{"data-role" => "button", "data-transition" => "fade", :href => "/mobile/docs/buttons/events"} Events
        %p
          The following options apply to
          = succeed ":" do
            %strong all buttons
        %dl
          %dt
            %code corners
            %em boolean
          %dd
            %p.default default: true
            %p
              Applies the theme button border-radius if set to true. This option is also exposed as a data attribute:
              %code data-corners=&quot;false&quot;
            %pre
              %code
                :preserve
                  $('a').buttonMarkup(
                    { corners: "false" }
                  );
          %dt
            %code icon
            %em string
          %dd
            %p.default default: null
            %p
              Applies an icon from the
              = succeed "." do
                %a{:href => "/mobile/docs/buttons/buttons_icons"} icon set
              This option is also exposed as a data attribute:
              %code data-icon=&quot;star&quot;
            %pre
              %code
                :preserve
                  $('a').buttonMarkup(
                    { icon: "star" }
                  );
          %dt
            %code iconpos
            %em string
          %dd
            %p.default default: "left"
            %p
              Positions the icon in the button. Possible values: left, right, none, notext. The notext value will display an icon-only button with no text feedback. This option is also exposed as a data attribute:
              %code data-iconpos=&quot;left&quot;
            %pre
              %code
                :preserve
                  $('a').buttonMarkup(
                    { iconpos: "right" }
                  );
          %dt
            %code iconshadow
            %em boolean
          %dd
            %p.default default: true
            %p
              Applies the theme shadow to the button's icon if set to true. This option is also exposed as a data attribute:
              %code data-iconshadow=&quot;false&quot;
            %pre
              %code
                :preserve
                  $('a').buttonMarkup(
                    { iconshadow: "false" }
                  );
          %dt
            %code inline
            %em boolean
          %dd
            %p.default default: null (false)
            %p
              If set to true, this will make the button act like an inline button so the width is determined by the button's text. By default, this is null (false) so the button is full width, regardless of the feedback content. Possible values: true, false. This option is also exposed as a data attribute:
              %code data-inline=&quot;true&quot;
            %pre
              %code
                :preserve
                  $('a').buttonMarkup(
                    { inline: "true" }
                  );
          %dt
            %code shadow
            %em boolean
          %dd
            %p.default default: true
            %p
              Applies the drop shadow style to the button if set to true. This option is also exposed as a data attribute:
              %code data-shadow=&quot;false&quot;
            %pre
              %code
                :preserve
                  $('a').buttonMarkup(
                    { shadow: "false" }
                  );
          %dt
            %code theme
            %em string
          %dd
            %p.default default: null, inherited from parent
            %p
              Sets the color scheme (swatch) for all instances of this widget. It accepts a single letter from a-z that maps to the swatches included in your theme. By default, it will inherit the same swatch color as it's parent container if not explicitly set. This option is also exposed as a data attribute:
              %code data-theme=&quot;a&quot;
            %pre
              %code
                :preserve
                  $('a').buttonMarkup(
                    { theme: "a" }
                  );
        .ui-body.ui-body-e
          %p
            The following option applies only to
            = succeed "," do
              %strong form buttons
            which are automatically initialized by the framework:
        %dl
          %dt
            %code initSelector
            %em CSS selector string
          %dd
            %p.default default: "button, [type='button'], [type='submit'], [type='reset'], [type='image']"
            %p
              This is used to define the selectors (element types, data roles, etc.) that will automatically be initialized as form buttons. To change which elements are initialized, bind this option to the
              = succeed ":" do
                %a{:href => "/mobile/docs/api/globalconfig"} mobileinit event
            %pre
              %code
                :preserve
                  $( document ).bind( "mobileinit", function(){
                    $.mobile.button.prototype.options.initSelector = ".myButtons";
                  });
    / /content-primary
    .content-secondary
      .ui-body-b
        %div{"data-collapsed" => "true", "data-content-theme" => "d", "data-role" => "collapsible", "data-theme" => "b"}
          %h3 More in this section
          %ul{"data-dividertheme" => "d", "data-role" => "listview", "data-theme" => "c"}
            %li{"data-role" => "list-divider"} Buttons
            %li{"data-theme" => "a"}
              %a{:href => "/mobile/docs/buttons/types"} Button basics
            %li
              %a{:href => "/mobile/docs/buttons/buttons_icons"} Button icons
            %li
              %a{:href => "/mobile/docs/buttons/buttons_inline"} Inline buttons
            %li
              %a{:href => "/mobile/docs/buttons/buttons_grouped"} Grouped buttons
            %li
              %a{:href => "/mobile/docs/buttons/buttons_themes"} Theming buttons
  / /content
  .footer-docs{"data-role" => "footer", "data-theme" => "c"}
    %p &copy; 2011 The jQuery Project
/ /page
