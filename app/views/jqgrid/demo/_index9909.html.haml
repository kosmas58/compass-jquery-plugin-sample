-@title = "#{t('txt.jqgrid.name')} #{t('txt.jqgrid.demo.name')} - #{t('txt.jqgrid.mine.cedit')} - #{t('txt.jqgrid.mine.cedit4')}"
.demo
  %h3
    =image_tag("icons/22x22/grid.png", :border => 0)
    =@title
  %center
    %table#invheaders_demo9909.scroll{ :cellspacing => "0", :cellpadding => "0" }
    #invheaders_demo9909_pager.scroll{ :style => "text-align:center;" }

/ End demo
.demo-description
  %p
    This example show how we can add additional column into the grid to define the three actions for each row.
  %p
    This can be done at server side and at client side. This example shows a server side solution with jQuery UI icons.
  %p
    Added option
    %code
      rowActions
    to gridify with possible values:
    %code
      \:client
    as default and
    %code
      \:server
    for adding client or server side row actions.
/ End demo-description
