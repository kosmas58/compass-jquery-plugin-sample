-@title = "#{t('txt.jqgrid.name')} #{t('txt.jqgrid.demo.name')} - #{t('txt.jqgrid.demo.row')} - #{t('txt.jqgrid.demo.cedit')}"
.demo
  %h3
    =@title
  %center
    %table#invheaders_demo0502.scroll{ :cellspacing => "0", :cellpadding => "0" }
    #invheaders_demo0502_pager.scroll{ :style => "text-align:center;" }

/ End demo 
.demo-description
  %p    
    This example show how we can add additional column into the grid to define the three actions for each row.
  %p
    This can be done at server side and at client side. This example shows a client side solution.
  %p
    To do that we use the 
    %code
      getDataIDs, setRowData
    and
    %code
      loadComplete
    methods.
  %p
    E - means edit, S - means Save and C - cancel Editing.   
/ End demo-description
