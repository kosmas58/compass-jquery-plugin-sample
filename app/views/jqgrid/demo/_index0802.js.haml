%style{:type => "text/css", :media => "screen"}
  :plain
    #toolbar { padding: 0.3em; }
    .demo-description td{ vertical-align: top; }

:javascript
  $(function() {
    $("a", ".demo").button();
  });
  
  $(document).ready(function() {
    jQuery("#bedata").click( function(){
    	var gr = jQuery("#invheaders_demo0802").jqGrid('getGridParam','selrow');
    	if( gr != null )
    	  jQuery("#invheaders_demo0802").jqGrid('editGridRow',gr,{height:280,reloadAfterSubmit:false});
    	else
    	 alert("Please Select Row");
    });
  });
  
= raw @grid