-@title = "#{t('txt.jqgrid.name')} #{t('txt.jqgrid.demo.name')} - #{t('txt.jqgrid.demo.row')} - #{t('txt.jqgrid.demo.types')}"
.demo
  %h3
    =@title
  %center
    %table#demo0505.scroll{ :cellspacing => "0", :cellpadding => "0" }
    #demo0505_pager.scroll{ :style => "text-align:center;" }

/ End demo 
.demo-description
  %p
    This example demonstrates a different input type. This is achieved via two new options:
  %p
    %p
      %code
        edittype
      where the possible values are text(default), checkbox, textarea and select.
    %p
      %code
        editoptions
      array which contain different options for the control.
  %p
    Try to click on some row.   
/ End demo-description
